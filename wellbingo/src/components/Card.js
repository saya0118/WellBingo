import React from 'react'

const Card = (props) => {
    return (
        <div>
            <button>

            </button>
        <button className="card">
            {props.value}
        </button>
        </div>
    )
}

export default Card;
